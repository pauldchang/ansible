---
- name: Install and start Ubuntu services
  hosts: ubuntu
  remote_user: ubuntu
  become: yes
  tasks:
    - name: Install Ubuntu packages
      apt:
        name: "{{ item }}"
        state: present
      loop:
        - nginx
        - apache2
        - mysql-server
        - php
        - redis-server

    - name: Start services
      systemd:
        name: "{{ item }}"
        state: started
        enabled: yes
      loop:
        - nginx
        - apache2
        - mysql
        - php
        - redis-server
